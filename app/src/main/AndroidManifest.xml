<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daomtthuan.aptechandroid">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.AptechAndroid">
    <activity android:name=".bookmanagement.activity.BookManagementEditActivity"></activity>
    <activity android:name=".bookmanagement.activity.BookManagementAddActivity" />
    <activity android:name=".bookmanagement.activity.BookManagementMainActivity" />
    <activity android:name=".carddistribution.activity.CardDistributionMainActivity" />
    <activity android:name=".studentlist.activity.StudentListMainActivity" />
    <activity android:name=".quadraticequation.activity.QuadraticEquationCalculateActivity" />
    <activity android:name=".quadraticequation.activity.QuadraticEquationMainActivity" />
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>